<div class="container mt-3">
    <div class="card align-items-center bg-light">
        <div class="card-block text-center pb-1">
            <h5 class="card-title">{{name}}</h5>         
            <ul class="list-group list-group-flush border border-3 border-info mb-2">
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Cuisine: <span class="badge bg-primary rounded-pill">{{cuisine}}</span></li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Price Range: <span class="badge bg-primary rounded-pill">{{price_range}}</span></li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Address: <span class="badge bg-primary rounded-pill">{{address}}</span></li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    Reviews: <span class="badge bg-primary rounded-pill">{{reviewCount}}</span></li>
            </ul>
            <h5>Average Overall Rating</h5>
            <input id="overall-rating" name="overall" class="rating" value="{{avgOverallRatings}}"  type="number" data-readonly="true" data-size="xs">
        <a href="/restaurants/reviews/{{id}}" type="submit" class="btn btn-primary" id="reviews-btn">See Reviews</a>
        <a href="/restaurants/post/{{id}}" type="submit" class="btn btn-primary" id="post-btn">Submit Review</a>
        </div>
    </div>
</div>